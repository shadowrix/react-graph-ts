{
  "name": "repo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "build:lib": "turbo run build --filter=react-graph-ts",
    "dev": "turbo run dev",
    "start:site": "turbo run dev --filter=site",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "turbo": "^2.6.1"
  },
  "packageManager": "pnpm@8.15.6"
}
